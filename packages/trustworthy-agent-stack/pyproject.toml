[project]
name = "trustworthy-agent-stack-minimal"
version = "0.1.0"
description = "Runnable minimal proof for TrustworthyAgentStack"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests", "python_validator"]
