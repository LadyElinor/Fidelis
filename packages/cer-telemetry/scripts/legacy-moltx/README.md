# Legacy MoltX scripts

These `tmp_moltx_*.mjs` files are retained as legacy scratch/research scripts.

They are not the clean packaged CER-Telemetry surface.
Prefer:
- `bin/moltx-trending.mjs`
- `npm run analyze:moltx`

Keep them here only until they are either retired, rewritten, or promoted into explicit package surfaces.
