{
  "name": "fidelis-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Polyglot workspace for the Fidelis governed-agent monorepo",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "check": "pnpm -r --if-present test"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.0"
}
